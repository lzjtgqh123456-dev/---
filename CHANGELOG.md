# 更新说明

## 0.9.3（2026-09-20）

### ✨ 新增：删除学期（方便进入下学期后重新导入课程）
- 学期设置里新增「已有学期」列表，每个学期都能删。
- **二次确认**：先算清楚影响面再问一次 ——「会同时删除 N 门课程、N 节课、N 份资料（含已加密的文件）」「删除后无法恢复」。
- 删除是彻底的：课程 / 课时 / 作业（含附件）/ 考试 / 课件笔记（**连加密文件一起清掉**），不留孤儿数据。
- 删的正好是当前学期时，会自动把剩下的最新一个设为当前。

### 🐛 修复
- **词典页底部被导航栏遮挡**：词典的联想/结果列表漏了底部留白（其它页面都有）。同时排查了搜索页（汇率 / 设置 / AI 搜索）的同类问题，一并修复。
- **词典下载完成后仍会闪一下「未安装 / 下载词典」提示**：① 下载成功后立刻切到「已就绪」；② 切换词典包时不再先把就绪状态清掉；③「未安装」不再弹 snackbar（卡片本身已经写清楚）。
- **离线翻译的识别语言写不清楚**：「中英」改成「中文 + 英文」、「俄文」改成「俄文（西里尔）」、自动档改成「自动判断（中／英／俄）」—— 写的就是这个模型实际认识什么。
- **学业管理的开学日期会误导人**：仍然自动对齐到周一（课表按周计算），但现在分开显示「你选的 X（周五）→ 第 1 周起点 Y（周一）」，并注明你选的日期不会被改动。
- **词典搜索排序：完全匹配排最前、一般匹配往后**
  - 俄→汉：保持「原形优先」；
  - 汉→俄（按释义反查）：改为按**义项**精确排序 —— 输入「书」时 book（释义以「书」开头）排第一，秘书 / 说明书这类只是"包含"的排最后；
    `<正, 书, 尤英>`、`[计]` 这类语体/学科标注不再参与匹配（否则会把 viz 这种误判成完全匹配）。

---

## 0.9.2（2026-09-20）

### 🐛 修复：证件／备忘录／学业里的日期滑轮
- **年份范围 2000~2100 → 1900~2100**：现在能选到 1900 年了（原来是硬编码从 2000 年起，历史证件根本选不了）。
- **滑轮不再循环**：滚到 1900 或 2100 就**停住**。原来 `wrapSelectorWheel = true`，滚到头会从另一头跳回来，
  想精确停在某个年份非常难受。「提前 N 天提醒」这类数字滑轮也一并改成不循环。
- **日随月份变化**：选 2 月不会再出现 31 号（如 1900-02 自动是 28 号）。

以上改动在共用的滑轮组件 `ui/WheelPicker.kt` 里，所以**证件（签发/到期日期）、备忘录、学业（作业截止/考试日期）一起生效**。

---

## 0.9.1（2026-09-20）

### 🚀 大文件打开/导入提速约 100 倍
- 根因：旧版把文件**直接交给 Android Keystore 加解密**。它每个操作都要过一遍 keystore 守护进程，
  实测只有 ~2.5 MB/s —— 打开 129MB 的视频要等一两分钟、445MB 的要 3 分钟；
  而且它的 GCM 实现内部会把输出**全部攒在内存里**，解密 134MB 就直接 OOM 崩溃
  （这就是「点了卡住、等很久之后报错」的真正原因）。
- 改为**信封加密**：真正的数据密钥（DEK）用 Keystore 的主密钥包一层存在本机，
  文件正文改用**软件 AES-GCM**（走 CPU 的 AES-NI 指令），并按 4MB 分块 ——
  既没有守护进程往返，内存峰值也只有几 MB。

  | 操作 | 旧版 | 0.9.1 |
  |---|---|---|
  | 导入 445MB 视频 | ~150 秒 | **~2 秒** |
  | 打开 445MB 视频 | ~180 秒 | **~1 秒** |
  | 打开 129MB 视频（升级后） | ~120 秒 | **<1 秒** |

- **安全性不变**：拿不到 Keystore 里的主密钥就解不开数据密钥，拷走文件/数据库一样打不开。

### ⬆️ 旧文件一键升级
- 启动时发现 **>4MB 的旧格式文件**会弹窗说明并询问是否升级；「以后再说」7 天内不再打扰。
- 升级在后台逐个进行并有进度显示，完成后提示「已升级 N 个文件」。
- 用「解密 → 重新加密到临时文件 → 改名覆盖」实现，**任何一步失败都不会动原文件**。
- 没手动升级的文件，在第一次打开时会自动升级（那一次仍需等待，之后就是秒开）。

### 🐛 修复
- **课程资料里大文件打不开**：见上面的 OOM 根因；同时把打开方式从"不可 seek 的管道 URI"
  换成"先解密成普通文件再交给系统 App"，PDF / Office / 视频播放器都能正常拖动进度条。
- **课程资料：添加后没有任何提示**：提示原本走主界面 Snackbar，被资料弹窗整个盖住，
  改为在弹窗里直接提示。
- **课程资料：添加后列表看不到刚加的文件**：弹窗订阅的是"当前查看课程"这个全局状态，
  从课程详情进入时没人设置它（列表恒为空）。改为弹窗直接订阅本课程的资料流。
- **图片查看器的「双指缩放 · 拖动查看」看不清**：原来直接把 75% 白字画在图片底部，
  还会被底部手势条压住。现在加了半透明底、避开系统栏，关闭按钮也避开了状态栏/刘海。
- **搜索与AI页**：删掉底部三行说明文字（"输入问题后点上方按钮"等）。

### 🔧 其它
- 缩略图改为下采样解码（原来整张解码，大图又慢又吃内存）。
- 打开/分享加密文件会临时解密到应用缓存，App 启动时自动清理。
- 修复旧格式文件解析时的一个头部判断错误（会导致旧文件全部解不开）。

---

## 0.9.0（2026-09-19）

- **离线 OCR 图片翻译**：相册选图 → PP-OCRv5（文本检测 + 中英识别 + 俄文识别，自动判定语言）→ 复用本地 GGUF 翻译模型。
  - 俄文印刷体真实手机截图 CER 从旧中英模型的 80.7% 降到 **2.0%**；模型首次使用时下载（约 22.7MB，多镜像 + SHA-256 校验）。
- **可自定义 App 图标**：把图片放到 `icon_src/icon.png`，跑 `tools/make_icon.py` 即生成全套图标（含自适应图标 / Android 13+ 主题图标）。
- 版本号 0.2.0 → 0.9.0。
